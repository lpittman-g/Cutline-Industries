# Optional XcodeGen spec. Or create an Xcode iOS App target manually with this bundle id.
name: ApprovalPing
options:
  bundleIdPrefix: studio.cutlineindustries
settings:
  MARKETING_VERSION: "1.0"
  CURRENT_PROJECT_VERSION: "1"
targets:
  ApprovalPing:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - ApprovalPing
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: studio.cutlineindustries.approvalping
      DEVELOPMENT_TEAM: REPLACE_WITH_APPLE_TEAM_ID
      CODE_SIGN_STYLE: Automatic
      INFOPLIST_FILE: ApprovalPing/Info.plist
    entitlements:
      path: ApprovalPing/ApprovalPing.entitlements
